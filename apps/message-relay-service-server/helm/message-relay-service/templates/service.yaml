apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-relay-service.fullname" . }}
  labels:
    {{- include "message-relay-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-relay-service.selectorLabels" . | nindent 4 }}
